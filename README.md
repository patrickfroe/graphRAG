# graphRAG state template

This repository now includes a basic state template for a graph-RAG style UI/runtime state object.

Use `state-template.json` as a starting point for wiring state management.
